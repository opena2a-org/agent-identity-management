# golangci-lint configuration for AIM
# Security-focused linting with gosec in audit mode.

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    # Security
    - gosec
    - bodyclose
    - sqlclosecheck
    - noctx

    # Code quality
    - govet
    - staticcheck
    - unused
    - ineffassign
    - typecheck
    - misspell
    - goimports

    # errcheck disabled — codebase has widespread unchecked returns.
    # TODO: enable and fix incrementally per-package.

linters-settings:
  gosec:
    # Audit mode: report all issues without filtering
    config:
      global:
        audit: true
    excludes:
      # Allow hardcoded credentials in tests
      - G101
    severity: medium
    confidence: medium

  govet:
    enable-all: true
    disable:
      - fieldalignment  # Too noisy for limited benefit

  misspell:
    locale: US

issues:
  exclude-dirs:
    - vendor
    - testdata
    - node_modules

  exclude-rules:
    # Allow unused params in interface implementations
    - linters:
        - unused
      source: "^func \\(.*\\) "

    # Relax security checks in test files
    - path: _test\.go
      linters:
        - gosec
        - bodyclose

    # CLI entrypoints — interactive prompts, bootstrapping
    - path: cmd/
      linters:
        - gosec

  max-issues-per-linter: 0
  max-same-issues: 0

output:
  formats:
    - format: colored-line-number
  sort-results: true
  sort-order:
    - linter
    - severity
    - file
